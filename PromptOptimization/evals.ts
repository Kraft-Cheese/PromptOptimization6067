// Evaluation for HellaSwag, PiQA, ARC, BoolQ, and GSM8K datasets

// 1/0 exact match evaluation for multiple-choice datasets (HellaSwag, PiQA, ARC, BoolQ)

// Numeric evaluation for GSM8K dataset (e.g., checking if the generated answer matches the correct numerical answer)

// Aggregate results and compute overall accuracy for each dataset

